<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @deck.name %>
</p>

<p>
  <strong>Detail:</strong>
  <%= @deck.detail %>
</p>

<%= link_to 'Add card', new_deck_card_path(@deck) %> |
<%= link_to 'Edit', [:edit, @deck] %> |
<%= link_to 'Back', decks_path %>
